{% extends 'base.html.twig' %}

{% block title %}Nouveau Appartement{% endblock %}

<div class="container">
    <h2 class="text-center mt-5 mb-3">Create appartement</h2>
    <div class="card">
        <div class="card-header">
            <a class="btn btn-outline-info float-right" href="{{ path('app_appartement_index') }}"> 
                View All appartement
            </a>
        </div>
        <div class="card-body">
             {{ include('appartement/_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
